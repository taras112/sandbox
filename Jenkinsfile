pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat 'set'
            }
        }
    }
post {
        always {
            echo 'This will always run'
        }
	}
}
